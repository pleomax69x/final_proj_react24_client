const getIsLoading = state => state.sprints.loading;
const getSprints = state => state.sprints.sprints;

// eslint-disable-next-line import/no-anonymous-default-export
export default { getIsLoading, getSprints };
